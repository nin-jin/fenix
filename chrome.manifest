# only for common environment: resource://fenix/this.jsm
# put this code into chrome.manifest with right path to fenix package
# for using fenix modules in your jsm use:
#   Components.utils.import( 'resource://fenix/this.jsm' )
#   const $fenix= $( 'relative/path/to/fenix/package/' )
# or use that in any context:
#   const $= Components.utils.import( 'resource://fenix/this.jsm', {} ).$
#   const $fenix= $( 'resource://path/to/your/extension/modules/fenix/' )

resource fenix ./ 
